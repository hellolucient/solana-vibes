# Cursor Rules

This directory contains cursor rules and guidelines for the project.

## Adding Your Rules

Place your `.md` files containing cursor rules in this directory. They will be automatically referenced by Cursor.

## Structure

- Each `.md` file can contain specific rules or guidelines
- Files are typically named descriptively (e.g., `react-native-rules.md`, `android-rules.md`, etc.)
